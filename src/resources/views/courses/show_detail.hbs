<div class="container text-center">
    <div class="p-5 mb-4 bg-light rounded-3">
        <div class="container-fluid py-5">
            <h1 class="display-5 fw-bold">{{course.name}}</h1>
            <p class="col fs-4">{{course.description}}</p>

            <img class="d-block mb-4 m-auto" src="{{course.image}}" alt="{{course.description}}" />
            <a href="/courses" class="btn btn-primary btn-lg" type="button">Trở về</a>


        </div>
    </div>
</div>